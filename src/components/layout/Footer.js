import React from 'react';
import '../../components/Footer/Footer.css';

// We won't show footer in iOS app-like interfaces
// as it's replaced by the Tab Bar, but keeping the component
// to maintain the structure
const Footer = () => {
  return null;
};

export default Footer;
